import { StyleSheet } from "react-native";

export const loadingTheme = StyleSheet.create({

  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    backgroundColor: 'white',

  },

  text: {
    fontSize: 16,
    fontWeight: '600',
    textAlign: 'center'
  },

})