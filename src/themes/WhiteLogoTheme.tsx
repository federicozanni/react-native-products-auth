import { StyleSheet } from "react-native";

export const whiteLogoTheme = StyleSheet.create({

  container: {
    alignItems: 'center',
    marginBottom: 10,
  },

  image: {
    width: 200,
    height: 200,
  },

})